[profile.default]
src = "src"
test = "test"
script = "script"
out = "out"
libs = ["lib"]
cache_path = "cache"
optimizer = true
optimizer_runs = 200
auto_detect_remappings = true
fs_permissions = [{ access = "read-write", path = "./deployments" }]
